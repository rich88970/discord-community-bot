# 虛擬投資使用方式與計算

`/invest` 使用遊戲代幣追蹤股票與 BTC 的價格，採**逐倉、線性合約**方式模擬多空交易。每個部位的預算獨立承擔損益，錢包與銀行存款不會自動被拿去補保證金。這不代表持有實際股票、加密資產或外幣，也不會送出真實交易訂單。

## 指令

| 指令 | 用法 |
|---|---|
| `/invest search` | 依名稱或代號搜尋 |
| `/invest price` | 查價格與行情時間，例如 `2330`、`AAPL`、`BTC` |
| `/invest buy` | 指定代號、預算、多空方向與 1–5 倍槓桿；預算至少 100，不設單筆或總本金上限 |
| `/invest portfolio` | 查看合計投入、費後權益估值、損益與參考強平價 |
| `/invest sell` | 用部位 ID 平倉，結算費用與損益 |
| `/invest transactions` | 查看開倉、加倉、平倉與強制平倉紀錄 |

投入金額必須小於等於錢包餘額。相同標的、方向、槓桿及費率的部位加倉時，按**數量加權**合併均價，累加投入預算與已支付的開倉費。程式不額外限制金額，但 Discord 數字欄位仍有平台本身的可表示範圍。

## 預算、部位大小與費用

預設模擬參數：

- 槓桿 `L`：1–5 倍。
- 開倉與平倉費率 `f`：各為成交名目價值的 **0.055%**。
- 維持保證金率 `m`：目前名目價值的 **0.5%**，使用單一模擬費率，沒有交易所的風險級距。
- 價格：Yahoo Finance 報價，快取 60 秒；強平檢查每輪結束後間隔 60 秒。

手續費參考線性衍生品按成交名目價值收費的方式，0.055% 參考 Bybit 公開的 VIP 0 taker 基準費率；本程式將它固定為可設定的模擬參數，不依帳號等級或地區動態調整。[交易費率參考](https://www.bybit.com/en/help-center/article/Trading-Fee-Structure)

設投入的整數代幣預算為 `B`，進場價為 `P₀`，預算**包含開倉手續費**：

```text
名目價值 N = B ÷ (1/L + f)
部位數量 Q = N ÷ P₀
開倉手續費 F₀ = N × f
實際初始保證金 I = B − F₀ = N ÷ L
```

因此，固定預算下開倉費越高，可建立的部位越小，沒有另外向錢包追加扣款。這使用「名目價值除以槓桿」的初始保證金關係。[初始保證金參考](https://www.bybit.com/en/help-center/article/Initial-Margin-USDT-Contract)

例如投入 1,000、槓桿 5 倍、進場價 100：名目價值約 4,986.29、數量約 49.862887、開倉費約 2.742458，餘下約 997.257542 作為初始保證金。

## 多空損益與平倉

以目前價格或平倉價格 `P` 計算：

```text
做多價差損益 = Q × (P − P₀)
放空價差損益 = Q × (P₀ − P)
平倉手續費 F₁ = Q × P × f
費後權益估值 = max(0, B − F₀ + 價差損益 − F₁)
平倉入帳 = floor(費後權益估值)
實現淨損益 = 平倉入帳 − B
顯示報酬率 = 費後淨損益估值 ÷ B × 100%
```

槓桿已反映在 `Q`，**不會再把價差損益乘一次槓桿**。計算使用 Decimal，JSON 保存小數字串，只在代幣入帳時向下取整；交易紀錄保留費用與取整差額。[多空損益與槓桿關係參考](https://www.bybit.com/en/help-center/article/FAQ-Profit-Loss-Calculation)

各報價貨幣只用於該標的的價格尺度，盈虧最終以遊戲代幣結算。模擬數量不是實際持股數；不同市場沒有現實的外匯兌換部位或匯率收益。

## 維持保證金與強制平倉

```text
維持保證金 MM = Q × P × m
可用部位權益 = B − F₀ + 價差損益
觸發強平：可用部位權益 ≤ MM + 預估平倉手續費
```

這參考逐倉模式的維持保證金檢查，並預留平倉費。真實交易所會用標記價格與其他風險機制；本程式使用 Yahoo 報價作為模擬估值價格。[強制平倉機制參考](https://www.bybit.com/en/help-center/article/UTA-Trading-Rules-Liquidation-Process)

Bot 會在背景定期檢查，查看持倉或餘額、交易時也會檢查相關部位。觸發後以取得的價格強制平倉，退回扣費後剩餘金額，從持倉中移除並記錄；不會在價格反彈後恢復該部位。跳空使損失超過部位預算時，領回為 0，額外缺口由遊戲模擬承擔，不扣其他資產。

參考強平價在單一維持保證金率、費率不變時為：

```text
做多：max(0, (Q × P₀ − I) ÷ (Q × (1 − m − f)))
放空：(Q × P₀ + I) ÷ (Q × (1 + m + f))
```

這是**定期查價的模擬**。Bot 關機、網路失敗、延遲行情或兩次檢查之間的價格越線後反彈，可能無法被觀察到；不能保證重現交易所的即時強平。行情失敗不會用虛構價格平倉；早於部位最近使用時間的報價也不會拿來結算。

## 模擬範圍

股票、BTC 都共用上述線性合約模型，不等同各市場實際的融資融券資格或規則。沒有模擬委託簿、成交量限制、滑價、標記價格、動態維持保證金級距、資金費率、借券／融資利息、股息、拆股、稅費或匯率。上述機制均可能使真實交易結果與本程式不同。

費率與檢查間隔位於 `config.py`。修改設定後請重新啟動 Bot；已建立部位保存其費率，以便其持倉估值、平倉與交易紀錄一致。
